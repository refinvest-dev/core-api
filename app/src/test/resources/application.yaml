refinvest:
  id:
    node-id: 0
  security:
    issuer: https://test.refinvest.local
    audience: refinvest-core-api-test
    jwt-secret: 01234567890123456789012345678901
    web-origin: http://localhost:3001

spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret
            scope: openid

management:
  endpoint:
    health:
      show-details: always
