# Activate explicitly in a private development or verification environment.
# The dedicated scrape account requires REFINVEST_METRICS_SCRAPE_PASSWORD.
server:
  address: 127.0.0.1

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
